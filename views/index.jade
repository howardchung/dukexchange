extend layout

block content
  .row
    .col-xs-12
      ul.grid-listing
        - each listing in listings
          li.grid-listing-item
            a(href='/listings/' + listing._id)
              .grid-listing-image(style="background-image: url('data:image/jpg;base64," + listing.image + "')")
                .grid-listing-detail
                  p= listing.title
